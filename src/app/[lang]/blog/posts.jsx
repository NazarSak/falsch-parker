import React from 'react';
import { PostList, TagList } from './components';

const Posts = ({}) => {
  return (
    <section>
      <TagList />
      <PostList />
    </section>
  );
};

export { Posts };
